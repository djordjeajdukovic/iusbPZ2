<Window x:Class="NetworkService.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:NetworkService.Views"
        xmlns:viewModel="clr-namespace:NetworkService.ViewModel"
        mc:Ignorable="d"
        Title="NetworkService - Wireframe CG4 T1 G3 P1" Height="720" Width="1180"
        MinHeight="650" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource AppBackgroundBrush}">

    <Window.InputBindings>
        <KeyBinding Key="D1" Modifiers="Control" Command="{Binding NavCommand}" CommandParameter="entities" />
        <KeyBinding Key="D2" Modifiers="Control" Command="{Binding NavCommand}" CommandParameter="display" />
        <KeyBinding Key="D3" Modifiers="Control" Command="{Binding NavCommand}" CommandParameter="graph" />
    </Window.InputBindings>

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:NetworkEntitiesViewModel}">
            <views:NetworkEntitiesView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:NetworkDisplayViewModel}">
            <views:NetworkDisplayView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:MeasurementGraphViewModel}">
            <views:MeasurementGraphView />
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#1F2937" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="NetworkService" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,16,0" />
                    <TextBlock Text="CG4 / T1 / G3 / P1 wireframe" Foreground="#D1D5DB" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Entiteti  Ctrl+1" Command="{Binding NavCommand}" CommandParameter="entities" ToolTip="Tabela, P1 pretraga, CG4 filter i forma za dodavanje" />
                    <Button Content="Mreža  Ctrl+2" Command="{Binding NavCommand}" CommandParameter="display" ToolTip="Drag&amp;Drop mreža sa 12 Canvas polja" />
                    <Button Content="Grafikoni  Ctrl+3" Command="{Binding NavCommand}" CommandParameter="graph" ToolTip="G3 grafikon i CG4 procentualni grafikon" />
                </StackPanel>
            </Grid>
        </Border>

        <ContentControl Content="{Binding CurrentViewModel}" />
    </DockPanel>
</Window>
